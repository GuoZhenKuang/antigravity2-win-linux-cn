# 更新日志

## [2.4.3] - 2026-08-02

### 新增汉化翻译
- 全面深度清洗字典文件（`common.json`、`page_settings.json`、`v2.3.1.json`），清理了多余的英文扩写后缀（如 `(Artifact)`、`(MCP)`、`(Worktree)` 等），提升界面纯中文一致性。
- 新增快捷键输入提示汉化（如 `Enter 立即发送`、`Alt+Enter 在当前轮次后排队`、`Alt+Enter 当提示词为空时，发送队列中的下一个`）。
- 新增模型分组说明与项目选择提示汉化（如 `此组内的模型: ...`、`选择项目`）。
- 新增通用继承设置与按钮操作提示汉化（如 `继承您的通用设置`、`删除服务器`、`限时`、`停止智能体失败`）。
- 新增多项动态正则表达式拦截与优化：
  - 动态探索任务（如 `探索了 N 个文件，N 个任务` / `N 个任务`）
  - 动态错误捕获（如 `执行器当前未运行 (错误 ID: ...)`）
  - 计划任务运行提示（如 `所有计划任务均以 ... 模型运行。`）
  - 对话标记已读/未读、媒体时间与反馈身份提示汉化。

### 优化与修复
- 优化 Linux 环境下 Antigravity 安装路径的自动检测逻辑（新增对 desktop 文件和 PATH 的解析支持）。
- 修复由于节点切分导致的“工具已启用”（如 `29工具s enabled`）部分文本漏译/错位显示问题。

## [2.3.1] - 2026-07-17

### 新增汉化翻译
- 新增智能体执行、队列消息投递相关设置汉化。
- 新增 PostHog 与 gopls (Go 语言助手) 等多个 MCP 服务器详细说明汉化。
- 新增撤销操作提示、会话/工作区属性、任务及交付件状态等多处 UI 细节汉化。
- 新增故障排查链接提示（See our...）、“创建发布”（Create Release）按钮、系统高负载错误提示及动态错误 ID（Error ID:...）的汉化支持。
- 引入了动态执行步骤的多量词复合汉化匹配，彻底解决对话中思考（Thought for...）、工作时长（Worked for...）、执行命令（Ran...）以及探索资源（Explored N files, N searches, N commands）等多节点拼合和缩放显示时的漏译问题。
- 将主要对照字典文件重命名为 `v2.3.1.json` 以匹配新版本。

## [2.2.1] - 2026-07-13

### 新增汉化翻译
- 新增“选择思维等级”与 Gemini 3.5 Flash (Low) 推理模式的提示及设置汉化。
- 新增付费用户 Quotas Increased（配额增加）及每周配额重置相关提示汉化。
- 新增项目创建引导界面、项目权限说明等汉化。
- 新增会话面板宽度（Conversation Width）、窄/宽（Narrow/Wide）设置项汉化。
- 新增 Token 消耗细分展示（Show 1 breakdown/Hide breakdown）汉化。
- 新增 Google Maps Platform 模版集成及路线路由卡片信息汉化。
- 新增应用自动更新检查设置（Automatic Check for Updates）汉化。
- 新增会话列表右键菜单操作（置顶、复制会话名称/ID、复制项目名称）汉化。
- 新增设置项目名称（Set Project Name）与环境选择器（Environment Selector）提示汉化。

### 修复与优化
- 修正了 Google Cloud Quotas 在原字典匹配时因空格排印导致的漏译问题，补全了该 MCP 卡片及标题的汉化。
- 优化了动态文本的汉化引擎，在 `localization_engine.js` 中新增了对以下带数值的动态字符串的正则拦截，彻底解决漏译问题：
  - 活跃对话数（如 `1 active conversation`）
  - 已归档对话数（如 `0 archived conversations`）
  - 正在运行的任务数（如 `1 task running`）
  - 已更改的文件数（如 `2 files changed`）
